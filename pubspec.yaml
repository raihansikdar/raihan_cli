name: raihan_cli
description: "raihan_cli generates clean Flutter feature folders with MVC or MVVM architecture, supporting GetX, Provider, Riverpod, and BLoC state management."
version: 2.0.0
homepage: https://github.com/raihansikdar/raihan_cli

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

publish_to: 'none'

executables:
  raihan_cli: raihan_cli

dependencies:
  flutter:
    sdk: flutter
    

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  test: ^1.25.15